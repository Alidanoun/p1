# التقرير الأمني والتقني المتكامل - الجزء الثالث (المراحل 7 إلى 8)
## التدقيق الأمني الشامل والتحليل التجاري للمنظومة

---

# المرحلة السابعة: التدقيق الأمني ونقاط الضعف (Security Audit)

يوضح هذا القسم تفاصيل تطبيق الجوانب الأمنية وثغرات التشفير وحوكمة الجلسات في المنظومة.

## 1. التوثيق الأمني وتصميم الحماية (Security Controls)

### أ. إدارة الجلسات وتجديد التوكنز (JWT & Session Governance)
* **تقسيم الرموز (Token Division)**: يُستخدم نظام تدوين الرموز المزدوج. يتم التحقق من صحة توكن الوصول `AccessToken` الموقّع بمفتاح RSA (خوارزمية RS256) للتأكد من هويته وصلاحية إصداره.
* **آلية دوران الرمز وتأمين التحديث (Refresh Token Rotation)**: يتم تخزين الـ `RefreshToken` بجدول `RefreshToken` كمعرّف UUID فريد. عند طلب العميل لتوكن وصول جديد، يتم إبطال الرمز القديم وتوليد رمز جديد وحفظه بالجدول. في حال تم الاستيلاء على رمز التحديث واستخدامه مرتين، يكتشف السيرفر العملية فوراً ويلغي كافة الجلسات النشطة لأسرة التوكن (`tokenFamily`) كإجراء وقائي فوري.
* **إبطال الجلسات الفوري (Instant Revocation)**: عند حدوث أي تعديل أمني بالهوية (مثل تعديل كلمة المرور أو الأدوار)، يتم زيادة حقول (`tokenVersion`, `authVersion`, `permissionVersion`) في جدول المستخدمين. يتحقق وسيط التوثيق `authenticateToken` بملف [auth.js](file:///c:/Users/User/Desktop/p4/al_markazia_backend/src/middleware/auth.js) من تطابق الإصدار الموجود بالتوكن مع قاعدة البيانات؛ وفي حال اختلف، تُلغى الجلسة فوراً ويُطلب تسجيل الدخول.

### ب. محدد معدل الطلبات والتنزيل (Rate Limiting)
* يطبق السيرفر في [advancedRateLimiter.js](file:///c:/Users/User/Desktop/p4/al_markazia_backend/src/middleware/advancedRateLimiter.js) محددات معدل طلبات صارمة معزولة حسب نوع الخدمة:
  * `loginLimiter`: يسمح بـ 10 محاولات فقط كل 15 دقيقة على مستوى الآي بي وحساب البريد الإلكتروني معاً لمنع التخمين الذكي.
  * `otpLimiter`: يسمح بـ 5 طلبات كود تحقق فقط كل 10 دقائق لتجنب تضخم فواتير إرسال الرسائل الهاتفية.
  * `uploadLimiter`: يسمح بـ 30 عملية رفع ملفات فقط كل ساعة للحد من استهلاك المساحة التخزينية.

### ج. الحماية ضد هجمات الويب (Web Vulnerabilities Mitigation)
* **حقن الأكواد وقاعدة البيانات (SQL Injection Protection)**: لا يعتمد السيرفر على استعلامات نصية مبنية يدوياً، ويستخدم بالكامل `Prisma ORM` الذي يقوم تلقائياً بتحويل الاستعلامات لمعاملات مجهزة (Parameterized Queries) تفصل البيانات عن الأوامر وتمنع الهجوم بالكامل.
* **حقن السكربتات والـ XSS**: تُمرر مدخلات المستخدم على وسيط تعقيم يعتمد على مكتبة `xss` لمنع حقن وسوم HTML أو أكواد Javascript في قاعدة البيانات.
* **تزوير الطلبات العابر للمواقع (CSRF Protection)**: يطبق السيرفر وسيطاً مخصصاً يعتمد على آلية `Double Cookie Submit`. يُقحم الخادم رمزاً سرياً عشوائياً كوكيز `XSRF-TOKEN` ويطلب من تطبيق العميل إرسال نفس الرمز في ترويسة الطلب `X-XSRF-TOKEN`. يتحقق الخادم من تطابقهما في جميع طلبات التعديل لمنع الهجوم.

---

## 2. الثغرات ونقاط الضعف الحقيقية المكتشفة من الكود (Vulnerabilities & Risk Levels)

### 1. تخزين رمز البين كود بالنص الواضح في نموذج المستخدم (Plaintext PIN)
* **الملف المتأثر**: `al_markazia_backend/prisma/schema.prisma` السطر 60.
* **تفاصيل الثغرة**: يحتوي نموذج `User` على الحقل `plainPin` لتخزين الرمز السري للموظف بصيغة نصية واضحة غير مشفرة، مما يعني إمكانية تصفح جميع رموز الأمان للمشرفين والمدراء بمجرد الوصول لقاعدة البيانات.
* **مستوى الخطورة**: **حرجة جداً (Critical)**.

### 2. غياب التحكم بصلاحيات تصفح الملفات الثابتة (Static Assets ACL Gaps)
* **الملف المتأثر**: `al_markazia_backend/server.js` السطر 194.
* **تفاصيل الثغرة**: يتم تقديم ملفات المجلد `uploads` بشكل مباشر للعامة بمجرد التحقق من صيغة الملف بالـ RegEx للصور، دون التحقق من هوية المشاهد، مما يتيح تصفح فواتير أو إيصالات مالية أو مستندات حساسة للعملاء بمجرد تخمين رابط الملف.
* **مستوى الخطورة**: **مرتفعة (High)**.

### 3. خوارزمية تشفير XOR بسيطة لتخزين المهام دون اتصال (Weak Local Encryption)
* **الملف المتأثر**: `al_markazia_app/lib/services/offline_queue_service.dart` السطر 19-36.
* **تفاصيل الثغرة**: يُشفر التطبيق معطيات الطلبات المعلقة محلياً بهاتف العميل باستخدام عملية XOR بسيطة مع مفتاح نصي ثابت مكشوف بالملف. يسهل فك التشفير واستخراج تفاصيل الطلبات والزبائن بسهولة من ملف التخزين المحلي للهاتف.
* **مستوى الخطورة**: **متوسطة (Medium)**.

---

# المرحلة الثامنة: التحليل التجاري ونموذج العمل (Business Reverse Engineering)

## 1. جوهر المنتج البرمجي (The Core Business Product)
* النظام عبارة عن **منظومة رقمية متكاملة لإدارة المطاعم متعددة الفروع ونقاط البيع (Restaurant Operating System & CRM)**.
* يوفر النظام حلاً ثلاثي الأبعاد:
  1. بوابة طلب مأكولات وتجربة مستخدم للعملاء النهائيين (عبر تطبيق الجوال والولاء).
  2. لوحة إدارة متكاملة للمطابخ ومراقبة زمن SLA وتسيير وتوجيه الطلبات.
  3. نظام CRM مصغر لتتبع ورعاية الفرص البيعية الكبرى والعملاء المحتملين للفروع (مثل طلبات توريد الحفلات أو وجبات الشركات الكبرى).

## 2. مستخدمو المنظومة المستهدفون (Target Users)
* **عملاء التجزئة (B2C)**: لطلب وجبات الطعام مباشرة، وتجميع نقاط المكافآت، والشحن المالي للمحفظة وتتبع التوصيل.
* **مدراء الفروع والمطابخ (Branch Staff)**: لمعالجة الطلبات، وتجهيز منيو المطبخ الحقيقي، والتحكم بساعات العمل اليومية.
* **إدارة الشركة والمسوقين (CRM Users)**: لإطلاق حملات الخصم والولاء، ومتابعة فرص المبيعات، ومطابقة التقارير المالية وسجلات التدقيق.

## 3. تقييم جاهزية المنتج للسوق المؤسسي ونظام المستأجرين (SaaS & Multi-Tenant Readiness)
* **هل يدعم الـ SaaS وتعدد المستأجرين حالياً؟**
  * **لا، ليس بشكل كامل**. البنية التحتية الحالية مصممة ومبرمجة لخدمة **مؤسسة واحدة فقط (Single Tenant)** تمتلك عدة فروع، وتتشارك جميع البيانات وتفاصيل المستخدمين نفس قاعدة البيانات والجداول دون عزل للمستأجرين على مستوى الخادم.
* **ما المتطلبات البرمجية لتحويله إلى SaaS تجاري متعدد الشركات؟**
  1. **عزل المستأجرين (Tenancy Isolation)**: إضافة نموذج `Tenant` بقاعدة البيانات، وربط جميع الجداول (فروع، أصناف، مستخدمين، طلبات) بحقل `tenantId` إلزامي لضمان العزل التام للمستندات والبيانات.
  2. **بنية RLS المحدثة**: تعديل سياسات RLS بقاعدة البيانات لتتحقق من معرّف المستأجر `app.current_tenant_id` بجانب معرّف الفرع لمنع تداخل الاستعلامات بين الشركات المشتركة.
  3. **بوابة الدفع ونظام الفوترة للشركات**: ربط النظام ببوابة شحن للمستأجرين (مثل Stripe Billing) لتفعيل الاشتراكات الشهرية التلقائية.

## 4. المكونات التجارية الناقصة في المنتج
1. **نظام لإدارة السائقين وتوزيع الوجبات التلقائي (Logistics Dispatcher)**: يفتقر النظام إلى لوحة مستقلة للسائقين تعتمد على حساب المسافات الجغرافية آلياً لإرسال الطلب لأقرب سائق متاح، ويتم التحكم بها حالياً بشكل بدائي أو خارجي.
2. **نظام الربط مع الطابعات الحرارية المباشرة بالشبكة (Direct IP Printing)**: تعتمد لوحة التحكم الحالية على طباعة المتصفح التقليدية؛ ولخدمة المطاعم المزدحمة، ينقص النظام إضافة مكتبة اتصال مباشر عبر بروتوكول Raw TCP / LPR لإرسال كود الطباعة ESC/POS للطابعات الحرارية بالشبكة المحلية فوراً عند قبول الطلب تلقائياً.
