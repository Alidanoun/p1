// lib/config/features.dart

class AppFeatures {
  /// 🔴 Disable driver tracking temporarily
  static const bool enableDriverTracking = false;
  
  /// 🟢 Keep customer location for branch distance/delivery calculation
  static const bool enableCustomerLocation = true;
}
