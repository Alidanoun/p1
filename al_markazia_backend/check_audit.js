const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const logs = await prisma.auditLog.findMany({ where: { action: { contains: 'BRANCH' } }, orderBy: { createdAt: 'desc' }, take: 10 }); console.log(JSON.stringify(logs, null, 2)); } run().finally(() => prisma.$disconnect());
